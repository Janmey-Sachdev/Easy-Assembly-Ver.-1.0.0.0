Animation Creator (Java)

Generates a sequence of Easy Assembly frames to simulate a simple animation.

Usage:
  javac src/AnimationCreator.java
  java -cp src AnimationCreator

Output: `animation.ea` (sequence of frames)
