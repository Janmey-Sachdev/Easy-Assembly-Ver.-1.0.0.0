HTML+CSS Styler (Java)

Simple converter that turns a minimal subset of HTML tags into `DISPLAY` lines and applies simple inline styles.

Usage:
  javac src/HtmlCssStyler.java
  java -cp src HtmlCssStyler input.html

Output: `styled_output.ea`
