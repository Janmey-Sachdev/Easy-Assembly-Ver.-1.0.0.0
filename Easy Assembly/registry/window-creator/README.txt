Window Creator (Java)

Run the Java tool to generate a small Easy Assembly program that draws an ASCII window.

Usage:
  javac src/WindowCreator.java
  java -cp src WindowCreator

Output: `window.ea`
